**AI Resources Consulted:** ChatGPT and ClaudeAI

It should be noted that all of the AI-generated content and citations in my project are documente:
1. Here in ATTRIBUTION.md 
2. in-line comments in my ipynb file that discuss at the code-level where AI content was used or AI was consulted (i.e. for debugging, conceptual questions, etc.).

**External Dataset**
The dataset used for this project is the same dataset from the research paper “Classification of melanoma skin Cancer based on Image Data Set using different neural networks”:  https://pmc.ncbi.nlm.nih.gov/articles/PMC11607306/. 
It can be found on kaggle: https://www.kaggle.com/datasets/hasnainjaved/melanoma-skin-cancer-dataset-of-10000-images

**Implementation of ResNet18**
The Resnet18 implementation consulted both ChatGPT and external website resource Geeks4Geeks for the implementation of teh ResNet18() class. The Geeks4Geeks website can be found here: https://www.geeksforgeeks.org/deep-learning/resnet18-from-scratch-using-pytorch/

CoLab autofill was useful for implementing the backbone, and ChatGPT generated the code within self.stem(). After this point, ChatGPT was intermittently used for debugging this class and ensuring that there were no overwrites/large errors in how the research paper was being implemented. CoLab autofill was also used in-line for building out this class.

**AI-Citations (Also available in the ipynb file):**

1. In[27]: The code in this cell to set a seed for this project is AI-generated by ChatGPT. I used ChatGPT for debugging for checking if my random seed was working (it initially was not), and ChatGPT suggested the code that is now in this code cell.
2. In[31]: The code used for train_transform is ClaudeAI-generated. From here, ClaudeAI was used for debugging, and generated the code that was edited for defining full_train_dataset and val_train_dataset.
3. In[33]: The code in this cell is partially generated by ChatGPT (That is, ChatGPT suggested the counter method for counting the class imabalnces). 
4. In[34]: The plot_training_curves() function is used from Homework 4
5. In[35]: The size of the fully connected layers fc1, fc2, and fc3 as well as the sizes for conv1, conv2, and conv3 were debugged using ChatGPT. I found it initially very difficult to translate the size of the convolutional layers from the research paper to my code and would get errors during training such as “mat1 and mat2 shapes are not the same”. ChatGPT helped with debugging this and generating the correct ordering and values found and used in super().__init__().
6. In[36]: I could not figure out how to save my models to later use them on test data, so ChatGPT generated the code for the syntax torch.save(model.state_dict(), "modelname.pth"), which is used in this code cell and all code cells where the CNN, ResNet18, EfficientNetB0, and ViT models are trained.
7. In[39]: The EfficientNetB0 implementation relied on a lot of consultation with ChatGPT for understanding how to implement Swish() (i.e. what it does conceptually) and Colab autofill was useful for the numerical values inside the MBConv(). ChatGPT suggested the use of self.stem() and self.chead() and the code for these is ChatGPT-generated. ChatGPT was also used for debugging this class as I initially had incorrect values for the last few MBConv() invokings inside the My_EfficientNetB0() class.
8. In[45]: ChatGPT (as mentioned earlier) was useful in saving my models for later testing. ChatGPT generated the code within this code cell and helped me make sure my models were properly saved and later loaded for testing.
9. In[46] Claude AI was incredibly useful in generating the code in this cell. I initially did not code the evaluate_model() function properly and this caused a lot of errors when I tried to implement a confusion matrix. The code in this cell is partially Colab autofill for helping me write these methods and ClaudeAI generated the code is this cell.
10. Although I already included this earlier, it is worth mentioning that Colab autofill is enabled on my Colab, so sometimes in-line Colab would write out/finish my line for me. Similarly, I touch on this earlier but also want to mention that for my class() model implementations ChatGPT was used to understand conceptually what the research paper implemented (eg. Find where this research paper it is stated that they used skip connections. Can you tell me where in this paper it is explicitly mentioned?) 
